

## 🔹 Базовый URL

```
http://127.0.0.1:8000/api/
```

Все эндпоинты требуют JWT токен, кроме получения токена. - [Как получить токен](./auth.md)


## 🔹 Метрики (Metric)

### GET `/api/metrics/`

Список метрик текущего пользователя.

**Ответ:**

```json
[
  {
    "id": 1,
    "name": "Продажи",
    "description": "Ежедневные продажи",
    "created_at": "2026-02-04T10:00:00Z"
  }
]
```

### POST `/api/metrics/`

Создание новой метрики.

**Запрос:**

```json
{
  "name": "Трафик",
  "description": "Количество посетителей"
}
```

**Ответ:**

```json
{
  "id": 2,
  "name": "Трафик",
  "description": "Количество посетителей",
  "created_at": "2026-02-04T10:05:00Z"
}
```

---

## 🔹 Тэги (Tag)

### GET `/api/tags/`

Список всех тегов (общие для всех пользователей).

**Ответ:**

```json
[
  {
    "id": 1,
    "name": "важное"
  },
  {
    "id": 2,
    "name": "срочно"
  }
]
```

---

## 🔹 Записи метрик (MetricRecord)

### GET `/api/metrics/{metric_id}/records/`

Список всех записей конкретной метрики (кэшируется).

**Параметры URL:**

* `metric_id` — ID метрики

**Ответ:**

```json
[
  {
    "id": 1,
    "metric": 1,
    "metric_name": "Продажи",
    "value": "1000.0000",
    "timestamp": 1675497600,
    "tags": [1, 2]
  },
  {
    "id": 2,
    "metric": 1,
    "metric_name": "Продажи",
    "value": "1200.0000",
    "timestamp": 1675584000,
    "tags": []
  }
]
```

### GET `/api/metrics/{metric_id}/records/{record_id}/`

Детализация одной записи метрики.

**Параметры URL:**

* `metric_id` — ID метрики
* `record_id` — ID записи

**Ответ:**

```json
{
  "id": 1,
  "metric": 1,
  "metric_name": "Продажи",
  "value": "1000.0000",
  "timestamp": 1675497600,
  "tags": [1, 2]
}
```

### POST `/api/metrics/{metric_id}/records/`

Создание новой записи метрики.

**Параметры URL:**

* `metric_id` — ID метрики

**Запрос:**

```json
{
  "value": "1500.5000",
  "timestamp": 1675670400,
  "tags": [1]
}
```

**Ответ:**

```json
{
  "id": 3,
  "metric": 1,
  "metric_name": "Продажи",
  "value": "1500.5000",
  "timestamp": 1675670400,
  "tags": [1]
}
```

> После создания записи кэш списка метрик сбрасывается автоматически.

---

## 🔹 Примечания

* Все эндпоинты, кроме `/api/token/`, требуют JWT авторизации.
* `timestamp` — Unix timestamp (секунды с 1970 года).
* Поле `metric_name` дублируется для ускорения работы и кэшируется.
